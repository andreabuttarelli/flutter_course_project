import 'package:flutter/material.dart';

class Routed2 extends StatelessWidget {
  const Routed2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Routed 2'),
    );
  }
}
