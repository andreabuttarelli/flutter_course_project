import 'package:flutter/material.dart';

class Routed1 extends StatelessWidget {
  const Routed1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Routed 1'),
    );
  }
}
